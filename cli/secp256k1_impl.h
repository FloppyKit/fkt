#ifndef SECP256K1_IMPL_H
#define SECP256K1_IMPL_H

#include "secp256k1_src/secp256k1.h"

/* Provide the missing `secp256k1_context_create` etc. – they are already declared in the header,
   but we need to include the implementation. */
#endif