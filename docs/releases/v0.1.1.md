# FKT v0.1.1 — Keypath matrix freeze

**Date:** 2026-07-09  
**Tag:** `v0.1.1`  
**Branch at freeze:** `test-matrix/post-v0.1`  
**Prior release:** `v0.1.0`

## What this release means

**Signing perfect first for v0.1 keypath single-sig**, proven by real Sparrow fixtures + automated harness.

This is a **matrix / validation freeze**, not a claim of full roadmap (V2–V4 / Ark) completeness.

## In scope (proven)

| Feature | Evidence |
|---------|----------|
| P2WPKH sign + ECDSA verify | High goldens, `make test-sparrow-real-golden` |
| P2TR **keypath** | Byte-equal Sparrow on several cases |
| True mixed P2WPKH + P2TR inputs | Merged legs; both witness types present |
| Nested P2SH-P2WPKH | Partial-sig insert when key matches |
| Harness + committed goldens | 14 default-matrix goldens |
| Testnet seeds documented | `cli/tests/TEST_SEEDS.md` only |

## Explicitly out of scope (v0.1.1)

- Taproot **script-path** / leaf spends (hard-abort on tree until **v0.2**)
- Full P2WSH multisig finalize (partial_sig only)
- Ark/Bark live exit claims
- PWA / USB GUI product freeze
- Mainnet anything

## Verify

```bash
git fetch --tags origin
git checkout v0.1.1
cd cli && make clean && make && make test-sparrow-real-golden
# expect: PASS=14 FAIL=0 SKIP=1
```

## Next product line

**v0.2.0** — Taproot script-path single-sig leaves (BIP341 leaf sighash + control block witness).  
See: `docs/plans/2026-07-09-v0.2-script-path.md`

When v0.2 harness is green, bump version per policy (support bar met → tag).
